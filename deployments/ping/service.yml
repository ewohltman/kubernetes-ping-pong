---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: ping
  name: ping
  namespace: default
spec:
  externalTrafficPolicy: Cluster
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: ping
  sessionAffinity: None
  type: NodePort
