---
apiVersion: v1
kind: Service
metadata:
  name: ping
  labels:
    app: ping
spec:
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: ping
  sessionAffinity: None
  type: ClusterIP
