---
apiVersion: v1
kind: Service
metadata:
  name: pong
  labels:
    app: pong
spec:
  externalTrafficPolicy: Cluster
  type: NodePort
  ports:
    - name: http
      nodePort: 30002
      port: 30002
      targetPort: 30003
      protocol: TCP
  selector:
    app: pong
  sessionAffinity: None
