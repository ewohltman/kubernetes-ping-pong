---
apiVersion: v1
kind: Service
metadata:
  name: pong
  labels:
    app: pong
spec:
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: pong
  sessionAffinity: None
  type: ClusterIP
